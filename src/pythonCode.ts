// Loads the exact, audited 2,250-line crypto_dual_layer_bot.py directly via Vite's raw loader.
import pythonBotScript from '../crypto_dual_layer_bot.py?raw';

export const PYTHON_SCRIPT_CODE = pythonBotScript;
